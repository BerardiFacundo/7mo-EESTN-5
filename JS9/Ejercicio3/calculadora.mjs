class Calculadora { // Creamos la clase Calculadora con métodos multiplicar y dividir
    multiplicar(a, b) {
      return a * b;
    }
  
    dividir(a, b) {
      return a / b;
    }
  }
  
  export default Calculadora; // Exportamos la clase por defecto